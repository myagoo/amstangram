import styled from "styled-components"
import {
  background,
  border,
  color,
  flexbox,
  layout,
  position,
  space,
  typography,
} from "styled-system"

export const View = styled.div`
  ${space}
  ${layout}
  ${typography}
  ${color}
  ${flexbox}
  ${background}
  ${border}
  ${position}
`
